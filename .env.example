# =============================================================================
# Eurostar Tools - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
# Required variables are marked with [REQUIRED].
# =============================================================================

# -----------------------------------------------------------------------------
# Database [REQUIRED]
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://eurostar:eurostar_dev@localhost:5432/eurostar

# -----------------------------------------------------------------------------
# Redis [REQUIRED]
# -----------------------------------------------------------------------------
# Redis connection string for BullMQ job queue
# Format: redis://[password@][host]:[port]
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# Authentication [REQUIRED]
# -----------------------------------------------------------------------------
# Secret key for JWT token signing (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-min-32-characters-long

# JWT token expiry duration (default: 7d)
# JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# Email Service [REQUIRED]
# -----------------------------------------------------------------------------
# Resend API key for sending notification emails
# Get one at: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# Email sender address (must be verified in Resend)
# EMAIL_FROM=notifications@yourdomain.com

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Node environment: development | production | test
NODE_ENV=development

# API server port (default: 3001)
PORT=3001

# API server host (default: 0.0.0.0)
# HOST=0.0.0.0

# CORS allowed origins (default: true for all origins in dev)
# Use comma-separated URLs in production: https://app.example.com,https://admin.example.com
# CORS_ORIGIN=true

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info

# Service name for logs (default: eurostar)
# SERVICE_NAME=eurostar

# -----------------------------------------------------------------------------
# GTFS Real-Time Feed
# -----------------------------------------------------------------------------
# Polling interval for GTFS-RT feed in milliseconds (default: 30000 = 30s)
# GTFS_POLL_INTERVAL_MS=30000

# Delay monitoring interval in milliseconds (default: 300000 = 5min)
# DELAY_MONITOR_INTERVAL_MS=300000

# GTFS-RT API key (if required by the feed provider)
# GTFS_RT_API_KEY=

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------
# Maximum requests per minute per user (default: 100)
# RATE_LIMIT_MAX=100

# Rate limit time window in milliseconds (default: 60000 = 1min)
# RATE_LIMIT_WINDOW_MS=60000
