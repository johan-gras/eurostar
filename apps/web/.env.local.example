# =============================================================================
# Eurostar Tools - Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development.
# In production, set these via your deployment platform.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Public API URL for client-side requests (browser)
# This is exposed to the browser via NEXT_PUBLIC_ prefix
NEXT_PUBLIC_API_URL=http://localhost:3001

# Internal API URL for server-side requests (SSR, API routes, Server Actions)
# In Docker/production, this can be the internal service URL
API_URL=http://localhost:3001
